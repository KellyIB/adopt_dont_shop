<h1><%= link_to "Shelter Index", "/shelters" %></h1>
<h2><%= link_to "Pets Index", "/pets" %></h2>
<br></br>

<h3>Pet List</h3>
<% @shelter_pets.pets.each do |pet| %>
  <h2>Meet: <%= link_to "#{pet.name}", "/pets/#{pet.id}"%></h2>
  <p><%= image_tag(pet.image) %></p>
  <p>Approximate Age= <%= pet.approximate_age %></p>
  <p>Description= <%= pet.description %></p>
  <p>Gender: <%= pet.sex %></p>
  <p>Adoption Status: <%= pet.adoption_status %></p>
  <br></br>
  <%= link_to "Update Pet", "/pets/#{pet.id}/edit" %>
  <br></br>
  <%= link_to "Delete Pet", "/pets/#{pet.id}", method: :delete %>
<% end %>
<br></br>
<%= link_to 'Create Pet', "/shelters/#{@shelter_pets.id}/pets/new" %>
<br></br>
<h2><%= link_to 'Pets', "/shelters/#{@shelter_pets.id}/pets" %></h2>
